import { AboutTimeline } from '@/components/about/about-timeline';

export const metadata = {
  title: 'About - Edzio\'s Portfolio',
  description: 'The story behind the code. Learn about Edzio\'s journey, achievements, and vision for the future.',
};

export default function AboutPage() {
  return <AboutTimeline />;
}